

// import { MODAL_STACK } from './modal.constants';

export interface ModalStackParams {
    
    // [MODAL_STACK.TIME_PICKER]: {
    //     selectedTime?: string; // 24 hour format (HH:mm / eg: 18:23)
    //     /**
    //      * @param time time in 24 hour format (HH:mm / eg: 18:23)
    //      */
    //     onSubmit?: (change: TimePickerChange) => void;
    //     maxTime?: Date;
    // };
   
  
}
